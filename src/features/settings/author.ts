/** Who made it. The avatar is GitHub's for the account, so it follows the profile picture. */
export const AUTHOR = { name: 'Nicolás Cabanillas', github: 'Nicoo01x' };
export const AUTHOR_URL = `https://github.com/${AUTHOR.github}`;
export const AUTHOR_AVATAR = `https://github.com/${AUTHOR.github}.png?size=128`;
/** The public repository (owner/name) whose contributors About lists; leave as is until it is published — the list simply stays empty. */
export const REPO = 'Nicoo01x/Zpace';
/** Where the installers are published (public, so the updater and the download page work while the code repo stays private). */
export const RELEASES_REPO = 'Nicoo01x/zpace-releases';
export const DOWNLOADS_URL = `https://github.com/${RELEASES_REPO}/releases/latest`;
